DEBUG=True
# Please regenerate secret key
SECRET_KEY = "django-insecure-0n%0oil&^q!^)jrv)og*k!2hz6*jqz(qnl%09p=8a7c9lvhdc@"

# Pin recommendations to one algorithm instead of the service's own A/B
# split. Valid values: collaborative, hybrid_content, pure_content.
# Defaults to "collaborative" if unset (see website/settings.py).
# RECOMMENDATION_SERVICE_VARIANT=collaborative # collaborative | hybrid_content | pure_content

# How many items GET /v1/recommendations/ returns by default, and the cap
# when the caller passes ?k=.
# RECOMMENDATION_DEFAULT_K=10
# RECOMMENDATION_MAX_K=100

# Where the recommendation service (see /recommendation) is reachable.
# Defaults to the host-mapped Docker port if unset.
# RECOMMENDATION_SERVICE_URL=http://localhost:8080

# Database
# DB_NAME=database
# DB_USER=root
# DB_PASSWORD=P@ssword